# 2026-08-25 Inventory Tracker Development Log

## Work Summary - 운영·계산 규칙 문서화

재고, 사용 주기, 구매 주기, 소비량 추정 규칙이 여러 문서와 코드에 흩어져 있어 현재 동작을 한곳에서 판단하기 어려웠다. 기존 구현과 테스트, 데이터베이스 제약을 대조해 `docs/inventory-rules.md`를 현재 규칙의 단일 기준 문서로 만들었다.

## Work Completed - 운영·계산 규칙 문서화

- 구매·입고·사용·개봉·소진·재고 정정의 의미와 재고 반영 여부를 구분했다.
- 모든 소비 계산을 개인 소비 기준으로 해석하고, 개봉형 사용 인원을 1인 환산에 반영하는 규칙을 명문화했다.
- 개봉형과 수량형의 표본 범위, 중앙값 계산, 용량·계절 보정 방식을 기록했다.
- 구매 간격 통계와 과거 구매량 기반 소비 추정이 서로 다른 계산이며 적용 범위도 다름을 정리했다.
- 소진 예측, 알림, 세일 구매 추천, 원장 재계산, 제품 숨기기·삭제 규칙을 모았다.
- 수량형 공동 사용과 과거 구매량에는 인원 보정 정보가 없다는 현재 모델의 한계를 명시했다.
- 올리브오일과 코코넛오일 사례를 포함한 대표 확인 사례를 추가했다.

## Decisions - 운영·계산 규칙 문서화

- `docs/inventory-rules.md`를 현재 운영·계산 규칙의 단일 기준으로 사용한다.
- 규칙 변경은 이 문서를 먼저 확정한 뒤 코드, 테스트, 화면 문구, 다른 문서를 순서대로 맞춘다.
- 앱 전체가 개인 소비 기준이라는 전제를 두므로 화면에 `1인 기준` 문구를 반복 표시하지 않는다.
- 이번 작업에서는 규칙 문서를 먼저 검토하려는 순서를 지키기 위해 README, `CONTEXT.md`, ADR, 기존 요구사항 문서를 수정하지 않았다.

## Current Status - 운영·계산 규칙 문서화

- 현재 코드, 단위 테스트, Supabase 마이그레이션에 구현된 규칙을 문서와 대조했다.
- 규칙 문서는 작성 완료했다.
- 코드, UI, 데이터베이스 동작은 변경하지 않았다.
- 다른 문서와의 불일치는 아직 정리하지 않았으며 다음 단계에서 이 규칙 문서를 기준으로 검토해야 한다.

## Next Steps - 운영·계산 규칙 문서화

- J님 검토를 통해 규칙 문서의 확정 내용과 수정할 내용을 구분한다.
- 수량형 공동 사용과 과거 구매량의 인원 보정 한계에 대한 처리 방식을 결정한다.
- 확정된 규칙을 기준으로 README, `CONTEXT.md`, ADR, 기존 요구사항 문서를 정비한다.
- 필요한 경우 코드와 테스트가 문서 규칙을 모두 충족하는지 별도 감사한다.

## Work Summary - 사용·재고 정정 정의 및 관련 문서 정비

규칙 문서를 검토하는 과정에서 수량형 제품의 `사용`과 `재고 정정`에 입력하는 숫자의 의미가 혼동될 수 있음을 확인했다. 두 행동의 기준을 구체화하고, 확정된 규칙과 현재 설명 문서의 불일치를 정리했다.

## Work Completed - 사용·재고 정정 정의 및 관련 문서 정비

- 운영·계산 규칙에 `사용`은 실제 소비한 증감량, `재고 정정`은 현재 실물 재고의 절대값이라는 정의를 추가했다.
- 쉬크 인튜이션 면도날 사례와 상황별 선택 표를 추가해 과거 총구매량과 현재 재고의 차이를 사용 기록으로 만들지 않도록 명시했다.
- `CONTEXT.md`에 구현 세부사항 없이 `사용 기록`과 `재고 정정`의 도메인 정의를 추가했다.
- README에 규칙·용어·ADR 문서의 역할을 연결하고, 현재 제공하는 소비·계절·세일 구매 기능을 반영했다.
- README의 구매 기록 설명을 실제 사용 우선, 과거 구매량 보조 추정 규칙에 맞게 수정했다.
- 2026-07-21 요구사항 분석 문서에 역사적 스냅샷 표시와 현재 기준 문서 링크를 추가했다.

## Decisions - 사용·재고 정정 정의 및 관련 문서 정비

- 사용량을 확실히 아는 경우에만 `사용`을 기록하고, 현재 잔량만 확실하면 `재고 정정`으로 기준점을 잡는다.
- 과거 구매량에서 현재 잔량을 뺀 값을 임의의 사용 기록으로 만들지 않는다. 소비 시점이 불명확한 기록이 사용 속도를 왜곡하기 때문이다.
- 과거 개발 로그와 요구사항 분석의 본문은 당시 사실과 사고 과정을 보존한다. 오래된 요구사항 분석은 삭제하거나 전면 개정하지 않고 현재 사양이 아님을 문서 상단에 표시한다.
- 1인 소비 기준 ADR은 현재 규칙과 일치하므로 수정하지 않는다.

## Current Status - 사용·재고 정정 정의 및 관련 문서 정비

- 규칙 문서, 도메인 용어집, README와 기존 요구사항 분석의 문서 역할을 정리했다.
- 코드, UI, 데이터 구조와 데이터베이스는 변경하지 않았다.
- 수정한 문서의 상대 링크, 코드 블록 짝, 후행 공백과 Git diff 형식을 검증했다.

## Next Steps - 사용·재고 정정 정의 및 관련 문서 정비

- 수량형 공동 사용과 과거 구매량의 인원 보정 한계에 대한 별도 규칙을 결정한다.
- 문서 검토 후 필요하면 버튼 문구나 입력 도움말을 같은 정의에 맞춘다.
