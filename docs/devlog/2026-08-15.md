# 2026-08-15 Inventory Tracker Development Log

## Work Summary

앱에서 사용자가 보거나 직접 입력하는 날짜를 미국식 순서로 통일했습니다. 저장과 날짜 계산에 사용하는 ISO 형식은 유지해 기존 Supabase 데이터와 계산 로직의 호환성을 보존했습니다.

## Work Completed

- `formatDate()`와 날짜 입력 보조 함수를 정리해 화면 표시를 `M/D/YYYY` 형식으로 변경했습니다.
- `M/D/YYYY`와 `MM/DD/YYYY`를 모두 인식하는 공통 `DateInput` 컴포넌트를 추가했습니다.
- 구매일, 기록 날짜, 개봉일, 사용 주기 날짜 입력을 공통 텍스트 입력 방식으로 교체했습니다.
- 과거 구매일 여러 줄 입력의 안내 예시와 입력 설명을 미국식으로 변경했습니다.
- 기존 `YYYY-MM-DD`, 점 구분, 한글 날짜 입력은 호환용으로 계속 인식하도록 유지했습니다.
- 백업 파일명에 포함되는 날짜도 미국식 순서로 변경했습니다.
- 날짜 정규화·표시·잘못된 날짜·기존 형식 호환 회귀 테스트를 추가했습니다.

## Decisions

- Supabase의 `date` 컬럼과 앱 내부 날짜 계산은 `YYYY-MM-DD`를 계속 사용합니다. 표시 형식을 데이터 형식으로 바꾸면 정렬·기간 계산·기존 데이터에 불필요한 위험이 생기기 때문입니다.
- 화면에 보이는 기준 형식은 `M/D/YYYY`로 정하고, 입력 시에는 앞자리 0이 있는 `MM/DD/YYYY`도 허용합니다.
- 기기와 브라우저 지역 설정에 따라 표시가 달라지는 네이티브 날짜 입력 대신 공통 텍스트 입력을 사용해 앱이 정한 입력 순서를 일관되게 유지했습니다.

## Current Status

- `npm.cmd run verify` 통과: Vite 빌드, 19개 테스트, Supabase 스키마 계약 검증.
- `git diff --check` 통과.
- 로컬 브라우저에서 로그인 화면과 콘솔 오류 없음까지 확인했습니다.
- 현재 브라우저 세션에 Google 로그인 상태가 없어 인증 후 재고 화면의 날짜 다이얼로그를 여는 실제 상호작용 검증은 완료하지 못했습니다.
- 기존 작업 중이던 `src/components/ProductCard.tsx` 변경은 수정하지 않았습니다.

## Next Steps

- 로그인 가능한 브라우저 세션에서 데스크톱과 iPhone 크기로 구매일·입고일·사용일·개봉일·사용 주기 입력을 한 번씩 확인합니다.
- 실제 앱에서 `2/10/2024`, `02/10/2024`, 잘못된 날짜, 미래 날짜 입력 결과를 최종 확인합니다.

## Work Summary - Hide Store Names from Item Summary

아이템 리스트 카드의 요약 줄에서 쇼핑몰 이름을 숨겨 제품명과 현재 재고 중심으로 보이게 했습니다. 구매처 설정값과 구매처별 묶음 기능은 유지했습니다.

## Work Completed - Hide Store Names from Item Summary

- `ProductCard`의 축약 요약 영역에서 쇼핑몰 이름 목록 렌더링을 제거했습니다.
- 펼친 상세의 쇼핑몰 정보와 과거 구매 기록의 쇼핑몰 정보는 유지했습니다.
- `groupByStore()`가 제품의 `store_ids` 설정값을 기준으로 계속 그룹을 만드는지 기존 회귀 테스트로 확인했습니다.
- 빌드, 19개 테스트, 스키마 계약 검증, `git diff --check`를 다시 실행했습니다.

## Decisions - Hide Store Names from Item Summary

- 이번 요청은 첨부 화면의 축약 아이템 리스트에 한정해 구매처 표시를 숨기는 것으로 해석했습니다. 상세 화면의 쇼핑몰 정보까지 제거하면 설정값을 확인하기 어려워질 수 있어 보존했습니다.
- UI 표시와 그룹화 로직을 분리했습니다. 따라서 카드에서 구매처를 보이지 않게 해도 `구매처별 보기`는 `product.store_ids`와 쇼핑몰 설정을 기준으로 동작합니다.

## Current Status - Hide Store Names from Item Summary

- `npm.cmd run verify` 통과: Vite 빌드, 19개 테스트, Supabase 스키마 계약 검증.
- `git diff --check` 통과.
- 브라우저에서 로그인 화면과 콘솔 오류 없음까지 확인했습니다.
- 인증 세션이 없어 로그인 후 아이템 리스트에서 실제 구매처 문구가 사라지는 화면 상호작용은 미검증 상태입니다.

## Next Steps - Hide Store Names from Item Summary

- 로그인 가능한 브라우저 세션에서 아이템 리스트와 `구매처별 보기`를 직접 확인합니다.

## Work Summary - Lucide Lab Icon Integration

글자가 많은 재고 화면을 빠르게 구분할 수 있도록 J님이 선택한 Lucide Lab SVG를 앱 제목, 설정 메뉴, 카테고리 그룹 제목에 연결했습니다.

## Work Completed - Lucide Lab Icon Integration

- `public/icons`에 추가된 집, 설정, 장바구니, 화장품, 생활용품, 영양제, 의복, 미분류 아이콘 디자인을 `Icons.tsx`의 React SVG 컴포넌트로 반영했습니다.
- `Header`의 `우리집 재고` 왼쪽에 집 아이콘을 추가하고 기존 문구는 유지했습니다.
- 설정 메뉴 버튼의 기존 설정 아이콘을 `settings.svg` 디자인으로 교체했습니다.
- 카테고리 보기에서 식료품·화장품·생활용품·영양제·의복·미분류 그룹명 앞에 각각 지정한 아이콘을 추가했습니다.
- 쇼핑몰별 보기에서는 쇼핑몰 이름 앞에 카테고리 아이콘이 붙지 않도록 조건을 분리했습니다.
- 데스크톱과 모바일에서 제목·카테고리 아이콘 크기가 화면에 맞도록 스타일을 추가했습니다.

## Decisions - Lucide Lab Icon Integration

- SVG를 단순 이미지로 표시하지 않고 기존 인라인 SVG 아이콘 구조에 맞춰 연결했습니다. 헤더의 흰색 아이콘과 카테고리별 색상·크기를 `currentColor`로 일관되게 제어할 수 있기 때문입니다.
- 아이콘은 의미를 보조하는 장식 요소로 처리하고 카테고리명과 앱 이름 텍스트는 유지했습니다. 따라서 아이콘이 로드되지 않거나 색상이 달라져도 핵심 정보는 사라지지 않습니다.

## Current Status - Lucide Lab Icon Integration

- `npm.cmd run verify` 통과: Vite 빌드, 19개 테스트, Supabase 스키마 계약 검증.
- 브라우저에서 앱 로그인 화면이 정상 렌더링되고 콘솔 오류·경고가 없음을 확인했습니다.
- 현재 브라우저 세션에 Google 로그인 상태가 없어 인증 후 재고 목록의 실제 카테고리 아이콘 배치는 직접 확인하지 못했습니다.

## Next Steps - Lucide Lab Icon Integration

- 로그인 가능한 브라우저 세션에서 카테고리 보기와 쇼핑몰별 보기를 데스크톱·모바일 크기로 한 번씩 확인합니다.

## Work Summary - Store Icon Integration

주구매처별 목록도 빠르게 구분할 수 있도록 쇼핑몰 그룹 이름 앞에 J님이 지정한 동물·자연물 아이콘을 추가했습니다.

## Work Completed - Store Icon Integration

- 로컬에 없던 `bird.svg`, `fish.svg`, `squirrel.svg`, `snail.svg`, `feather.svg`, `birdhouse.svg`, `shell.svg`를 공식 Lucide 아이콘 원본 기준으로 `public/icons`에 추가했습니다.
- 쿠팡·네이버·마켓컬리·아이허브·올리브영·자사몰·기타를 각각 지정된 아이콘 컴포넌트에 매핑했습니다.
- 쇼핑몰별 보기의 그룹 제목 앞에 주구매처 아이콘을 표시하고, 미지정 쇼핑몰은 기타용 조개 아이콘을 사용하도록 했습니다.
- 카테고리별 보기의 기존 아이콘과 쇼핑몰별 보기의 새 아이콘을 분리해 각 보기의 의미가 섞이지 않도록 유지했습니다.

## Decisions - Store Icon Integration

- 주구매처 아이콘도 기존 카테고리 아이콘과 동일하게 인라인 SVG 컴포넌트로 연결했습니다. 헤더·카테고리 아이콘과 같은 `currentColor` 제어와 크기 규칙을 공유하기 위해서입니다.
- 현재 저장된 쇼핑몰 이름이 지정 목록에 없더라도 목록이 깨지지 않도록 `기타` 아이콘을 안전한 기본값으로 사용했습니다.

## Current Status - Store Icon Integration

- 공식 Lucide SVG 원본을 기준으로 아이콘 파일과 React 매핑을 추가했습니다.
- `npm.cmd run verify` 통과: Vite 빌드, 19개 테스트, Supabase 스키마 계약 검증.
- 개발 서버에서 새 SVG 7개가 모두 정상 제공되는 것을 확인했습니다.
- 브라우저에서 로그인 화면과 콘솔 오류·경고 없음까지 확인했습니다.
- 로그인 세션이 없어 실제 쇼핑몰별 보기에서 그룹 앞 아이콘이 보이는 화면은 아직 직접 확인하지 못했습니다.

## Next Steps - Store Icon Integration

- 로그인 가능한 브라우저 세션에서 쇼핑몰별 보기의 각 그룹 아이콘과 카테고리별 보기의 기존 아이콘이 서로 바뀌지 않는지 데스크톱·모바일 크기로 확인합니다.

## Work Summary - Expanded Card Scroll Position

아이템 카드를 펼쳤을 때 긴 상세 영역의 아래쪽이 화면에 남아 무엇을 보고 있는지 헷갈리는 문제를 줄이기 위해 펼쳐진 카드의 상단으로 화면을 이동하도록 수정했습니다.

## Work Completed - Expanded Card Scroll Position

- `ProductCard`가 펼침 상태가 된 다음 한 프레임 뒤 카드 자체를 화면 상단 기준으로 스크롤하도록 처리했습니다.
- 상단 고정 헤더에 카드 제목이 가려지지 않도록 데스크톱·모바일별 `scroll-margin-top`을 추가했습니다.
- `prefers-reduced-motion` 설정이 켜진 환경에서는 부드러운 이동 대신 즉시 이동하도록 처리했습니다.

## Decisions - Expanded Card Scroll Position

- 키보드 포커스를 다른 요소로 강제로 옮기지 않고, 클릭한 카드의 상단이 보이도록 화면 스크롤만 조정했습니다. 따라서 기존의 펼침 버튼 접근성과 키보드 탐색 순서는 유지됩니다.
- 카드가 닫힐 때는 스크롤하지 않고, 카드가 펼쳐질 때만 이동하도록 제한했습니다.

## Current Status - Expanded Card Scroll Position

- `npm.cmd run verify` 통과: Vite 빌드, 19개 테스트, Supabase 스키마 계약 검증.
- `git diff --check` 통과.
- 브라우저에서 로그인 화면과 콘솔 오류·경고 없음까지 확인했습니다.
- 로그인 가능한 세션이 없어 실제 아이템 카드를 클릭한 뒤 카드 상단으로 이동하는 상호작용은 직접 확인하지 못했습니다.

## Next Steps - Expanded Card Scroll Position

- 로그인 가능한 브라우저 세션에서 화면 아래쪽의 아이템을 클릭해 카드 상단과 고정 헤더 사이에 제목이 보이는지 데스크톱·모바일 크기로 확인합니다.

## Work Summary - Collapse Groups by Default

빨간 상태 아이템이 있는 카테고리가 자동으로 펼쳐져 목록이 산만해지는 문제를 줄이기 위해, 카테고리와 주 구매처 그룹을 기본적으로 모두 접힌 상태로 표시하도록 수정했습니다.

## Work Completed - Collapse Groups by Default

- 빨간 상태 아이템 수 또는 `확인 필요` 필터를 기준으로 그룹을 자동 펼치던 조건을 제거했습니다.
- 첫 화면에서는 모든 카테고리·주 구매처 그룹이 접힌 상태로 시작하도록 변경했습니다.
- 검색어 입력, 그룹 제목을 직접 클릭하는 동작, 아이템 카드 펼침처럼 사용자가 명시적으로 내용을 확인하는 경우에는 기존처럼 필요한 그룹이 열리도록 유지했습니다.
- 그룹 제목에 표시되는 빨간 상태 아이콘과 `확인` 개수는 그대로 유지하고, 자동 펼침 동작만 변경했습니다.

## Decisions - Collapse Groups by Default

- 빨간 상태 아이템은 재고 데이터상 항상 존재할 수 있으므로, 해당 상태를 그룹 열림의 기본 조건으로 사용하지 않도록 했습니다.
- 사용자가 검색하거나 직접 그룹을 열었을 때만 내용을 펼쳐 정보량을 제어하고, 상태 요약은 접힌 그룹 제목에서 계속 확인할 수 있게 했습니다.

## Current Status - Collapse Groups by Default

- `npm.cmd run verify` 통과: Vite 빌드, 19개 테스트, Supabase 스키마 계약 검증.
- `git diff --check` 통과.
- 로컬 브라우저 로그인 화면 렌더링과 콘솔 오류·경고 없음까지 확인했습니다.
- Google 로그인 세션이 없어 실제 재고 목록에서 그룹을 클릭하는 상호작용 검증은 아직 완료하지 못했습니다.

## Next Steps - Collapse Groups by Default

- 로그인 가능한 브라우저 세션에서 카테고리별 보기와 주 구매처별 보기의 초기 상태가 모두 접혀 있는지 확인합니다.
- 검색·수동 그룹 열기·아이템 카드 펼침 시 해당 그룹만 정상적으로 열리는지 확인합니다.

## Work Summary - Clarify Purchase Count

재구매 간격 영역의 `8개`가 재고 수량처럼 읽히는 문제를 해결하기 위해 `8회 구매`로 표시를 명확히 했습니다. 과거 구매 기록이 구매 빈도와 실제 소비량 중 무엇을 설명하는지도 기존 요구사항과 현재 계산 코드를 대조했습니다.

## Work Completed - Clarify Purchase Count

- 최근 구매일 요약을 `N개`에서 `N회 구매`로 변경했습니다.
- 구매 기록, 입고 기록, 구매 기준일, 구매 횟수, 구매량, 실제 사용량의 의미와 경계를 루트 `CONTEXT.md`에 기록했습니다.
- 기존 Purchase Log 문서와 현재 계산 함수를 확인해 구매 수량·구매 당시 용량은 저장되지만 재구매 간격 계산에는 사용되지 않는 현재 동작을 확인했습니다.

## Decisions - Clarify Purchase Count

- 구매 횟수는 같은 날의 여러 구매·입고 기록을 한 번으로 합친 구매 기준일 수를 뜻하며, 제품의 총구매 개수나 현재 재고 수량과 구분합니다.
- 재구매 간격은 구매 행동의 주기를 나타내므로 날짜 간격 계산을 유지했습니다.
- 구매량은 대량 구매와 이월 재고 때문에 실제 사용량과 같지 않으므로 이번 변경에서는 소비량 계산에 바로 합치지 않았습니다. 실제 소비 속도는 사용 기록과 개봉·소진 주기를 우선 근거로 삼고, 구매 기록 기반 확보량은 별도 보조 지표로 설계해야 합니다.

## Current Status - Clarify Purchase Count

- `npm.cmd run verify` 통과: Vite 빌드, 19개 테스트, Supabase 스키마 계약 검증.
- 빌드 산출물에서 변경된 `회 구매` 문구가 포함되는 것을 확인했습니다.
- 변경은 로컬 작업 트리에만 있으며 배포하지 않았습니다.
- 전체 `git diff --check`는 기존 `ProductEditor.tsx`의 공백 두 곳 때문에 실패하며, 해당 사용자 변경은 수정하지 않았습니다.

## Next Steps - Clarify Purchase Count

- 구매 기록의 `구매 수량 × 제품 1개 용량`을 기간별 구매량으로 보여줄지 결정합니다.
- 실제 월 사용량은 완료된 사용 주기 또는 사용 이벤트를 우선 사용하고, 기록이 부족할 때만 구매량을 보조 추정으로 사용할지 결정합니다.

## Work Summary - Purchase Consumption and Sale Planning

과거 구매 기록이 많은 화장품에서 구매 간격보다 월평균 소비 용량과 다음 세일 구매 수량을 알고 싶다는 요구에 맞춰 구매·입고·소비 계산을 분리했습니다. 별도의 구매 유형 선택은 추가하지 않고 제품의 기존 개수/용량 기록 방식과 선택적 세일 계획을 사용하도록 구현했습니다.

## Work Completed - Purchase Consumption and Sale Planning

- 구매 기록 행 수, 총 구매 수량, 최근 실제 구매, 최근 입고를 각각 집계해 카드에 분리 표시했습니다.
- 실제 사용 기록이 있으면 이를 우선하고, 없으면 현재 재고 추적 시작 전의 과거 구매량을 이미 사용한 것으로 가정해 월평균·연간 소비량을 계산했습니다.
- 용량이 비어 있는 과거 구매 기록은 현재 제품 1개 용량을 적용하고, 다른 용량 단위 기록은 소비 용량 계산에서 제외하도록 했습니다.
- 다음 세일 날짜, 확보할 개월 수, 여유 재고를 제품 설정에 추가하고 세일일까지의 예상 재고를 뺀 추천 구매 수량을 계산했습니다.
- 과거 구매 일괄 입력에 구매 수량과 제품 1개 용량을 복원했습니다. 같은 날짜·쇼핑몰 기록이 있으면 경고하고, 실제 별도 구매임을 확인해야 저장할 수 있게 했습니다.
- 카드에는 최근 3건만 표시하고, 전체 기록은 연도별 합계와 같은 날 복수 구매 표시가 있는 스크롤 대화상자에서 확인·수정하도록 바꿨습니다.
- 세일 계획 열과 원자적 제품 생성·수정 RPC를 추가하는 `20260815042204_add_purchase_planning.sql` 마이그레이션과 스키마 계약 검증을 추가했습니다.
- 구매·입고·소비량 추정·세일 구매 계획의 의미를 `CONTEXT.md`에 정리했습니다.

## Decisions - Purchase Consumption and Sale Planning

- `일반 재구매형`과 `대량 세일 구매형`을 제품 유형으로 저장하지 않습니다. 실제 소비를 재는 방식과 물건을 사는 전략은 독립된 정보이기 때문입니다.
- 구매 간격은 실제 구매일만으로 계산하고 입고일은 포함하지 않습니다. 입고는 현재 재고 변화, 구매는 구매 행동을 뜻하도록 역할을 분리했습니다.
- 과거 구매량 기반 값은 확정 사용량이 아니라 전제를 둔 소비량 추정으로 표시합니다. 완료된 사용 주기나 사용 이벤트가 쌓이면 자동으로 실제 사용 기록 기준 계산이 우선됩니다.
- 세일 추천 수량은 `월평균 포장 수 × 확보 개월 + 여유 재고 - 세일일 예상 재고`를 올림해 계산합니다.

## Current Status - Purchase Consumption and Sale Planning

- `npm.cmd test`의 22개 계산 회귀 테스트, `npm.cmd run build`, `npm.cmd run test:schema`가 통과했습니다.
- 허블룸 콤부차 세럼 예시 데이터로 `8건 · 총 16통`, 월평균 약 `19.04ml`, 연간 약 `228.46ml`, 예시 세일 계획 추천 `5통`을 확인했습니다.
- 동일한 실제 React 컴포넌트를 사용한 임시 로컬 검증 화면에서 전체 구매 기록, 같은 날 2건, 중복 경고, 구매 기록 수정 연결, 세일 계획 입력값을 확인했습니다. 데스크톱과 390px 모바일 모두 콘솔 오류와 가로 넘침이 없었습니다.
- 검증용 임시 파일은 제거했습니다. 데이터베이스 마이그레이션은 로컬 파일만 생성했으며 운영 Supabase에는 적용하지 않았고 앱도 배포하지 않았습니다.

## Next Steps - Purchase Consumption and Sale Planning

- 운영 반영 시 데이터베이스 마이그레이션을 먼저 검토·적용한 뒤 같은 프런트엔드를 배포합니다.
- 실제 허블룸 과거 구매 기록에서 누락된 수량이나 당시 제품 용량이 있다면 전체 구매 기록 창에서 수정해 추정 정확도를 높입니다.
- 다음 블랙프라이데이 날짜와 몇 개월치를 확보할지 제품 설정에 입력해 실제 추천 구매 수량을 확인합니다.

## Work Summary - Purchase Planning Production Rollout

구매·소비 통계와 세일 구매 계획 기능을 운영에 안전하게 반영하기 위해 데이터베이스 변경을 프런트엔드보다 먼저 적용하고, 로컬 마이그레이션 이력을 운영 이력과 일치시켰습니다.

## Work Completed - Purchase Planning Production Rollout

- 운영 Supabase에 `add_purchase_planning` 마이그레이션을 적용했습니다.
- 운영에서 부여된 버전 `20260815042204`에 맞춰 로컬 마이그레이션 파일명과 스키마 계약 검증 목록을 정리했습니다.
- 적용 전후 `inventory_products` 17건과 `inventory_purchases` 28건이 유지되는지 확인했습니다.
- 세일 계획 열 3개, 제약 조건 3개, 제품 생성·수정 RPC 매개변수, `authenticated` 실행 허용과 `anon` 실행 차단을 운영 DB에서 확인했습니다.

## Decisions - Purchase Planning Production Rollout

- 새 프런트엔드가 호출할 RPC와 열이 먼저 존재하도록 데이터베이스를 선행 배포했습니다.
- 마이그레이션 파일 버전은 로컬 생성 시각이 아니라 운영 이력에 기록된 버전을 기준으로 맞춰 이후 이력 비교가 어긋나지 않게 했습니다.

## Current Status - Purchase Planning Production Rollout

- 운영 데이터베이스 반영과 계약 검증이 완료되었습니다.
- 공유 Supabase 프로젝트의 기존 보안·성능 권고는 남아 있지만, 이번 마이그레이션에서 추가한 제품 RPC는 익명 실행이 불가능하며 새 운영 차단 요인은 발견되지 않았습니다.
- 커밋 `4316336`을 `main`에 반영했고 GitHub Pages 배포, 데이터베이스 계약 검사, lockfile 검사가 모두 성공했습니다.
- 공개 앱의 허블룸 콤부차 세럼 카드에서 `8건 · 총 16통`, 월평균 `약 19.04ml`, 연간 `약 228.46ml`가 표시되는지 확인했습니다.
- 전체 구매 기록에서 2022년부터 2025년까지 8건이 모두 노출되고, 2023-07-06의 같은 날 2건이 별도 행과 중복 배지로 표시되는지 확인했습니다.
- 공개 앱은 HTML과 새 JS/CSS 자산이 모두 200으로 응답했고, 로그인된 Chrome 검증에서 콘솔 경고·오류와 프레임워크 오류 화면이 없었습니다.

## Next Steps - Purchase Planning Production Rollout

- 실제 허블룸 기록에서 용량이 비어 있는 4건의 당시 제품 용량이 현재와 달랐다면 전체 구매 기록에서 보정합니다.
- 다음 블랙프라이데이 날짜와 확보할 개월 수를 제품 설정에 입력해 운영 데이터 기준 추천 구매 수량을 확인합니다.

## Work Summary - Inventory Event Replay Corrections

`더랩 히알루론산 크림 50ml`의 입고 수량을 `3통` 대신 `2통`으로 저장한 뒤 보정용 `1통`을 별도 입력해야 했던 문제를 해결했습니다. 최신 기록 한 건만 고칠 수 있던 제한을 없애고, 어느 시점의 입고·사용 기록이든 수정하거나 삭제하면 이후 원장과 현재 재고가 자동으로 다시 계산되도록 변경했습니다.

## Work Completed - Inventory Event Replay Corrections

- 모든 제품 유형에서 과거 입고·사용 기록을 수정할 수 있도록 기존 최신 기록·개수 추적 제품 제한을 제거했습니다.
- 카드의 최근 기록 아래에 전체 재고 기록 진입 버튼을 추가하고, 전체 기록을 연도별로 확인해 수정·삭제할 수 있는 대화상자를 구현했습니다.
- 수정 수량이나 기록 삭제가 현재 재고와 이후 몇 건의 기록에 영향을 주는지 저장 전에 보여주는 미리보기를 추가했습니다.
- 제품별 재고 원장을 입력 순서대로 다시 계산하는 데이터베이스 함수를 추가했습니다. 입고·사용·개봉·소진 기록의 전후 수량을 갱신하고 제품의 현재 재고도 같은 트랜잭션에서 동기화합니다.
- 실제 재고 확인으로 생성된 정정 기록은 당시 확인한 재고 수량을 기준점으로 유지하도록 했습니다.
- 입고·사용 기록 수정·삭제 RPC는 로그인한 작업 공간 구성원만 실행할 수 있게 제한하고, 익명 실행 권한을 차단했습니다.
- 계산 미리보기 단위 테스트와 데이터베이스 계약 테스트에 `2통 + 1통`을 `3통` 한 건으로 바로잡는 회귀 시나리오를 추가했습니다.

## Decisions - Inventory Event Replay Corrections

- 사용자의 의도적인 기록 교정을 막는 대신, 변경 후 모든 종속 수량을 서버에서 원자적으로 다시 계산해 원장 불일치를 예방합니다.
- 재고의 인과 순서는 과거 날짜를 나중에 입력할 수 있다는 점을 고려해 구매일이 아니라 실제 기록 생성 순서로 유지합니다.
- 개봉·소진 제품의 수량은 정수만 허용하고, 재계산 중 어느 시점이라도 재고가 음수가 되거나 재고 없이 개봉하는 상태가 생기면 전체 변경을 취소합니다.
- 삭제는 별도의 확인 단계를 거치며, 삭제 후 결과 재고를 먼저 보여줍니다.

## Current Status - Inventory Event Replay Corrections

- `npm.cmd test`의 25개 계산 회귀 테스트, `npm.cmd run build`, `npm.cmd run test:schema`, `git diff --check`가 통과했습니다.
- 운영 스키마와 동일한 데이터베이스에서 전체 마이그레이션을 트랜잭션 안에서 적용한 뒤 롤백하는 컴파일 검증을 완료했습니다.
- 같은 롤백 트랜잭션에서 테스트 제품에 `2통`, `1통`을 입력하고 `1통` 기록 삭제와 `2통 → 3통` 수정 후 현재 재고 `3통`, 입고 기록 1건이 되는 것을 확인했습니다.
- 운영 Supabase에 `20260815144215_replay_inventory_event_corrections` 마이그레이션을 적용했습니다.
- 적용 전후 제품 26건, 재고 기록 68건, 크림 기록 2건과 현재 재고 3통이 그대로 유지되는 것을 확인했습니다.
- 새 수정·삭제 RPC의 로그인 사용자 실행 허용과 익명 실행 차단을 운영에서 확인했습니다. 공유 데이터베이스의 기존 보안·성능 권고는 남아 있으나 이번 함수에서 추가된 익명 노출은 없습니다.
- 공개 앱에서 크림 카드의 입고 2건, 전체 재고 기록 대화상자, `1통` 기록 삭제 미리보기의 `3통 → 2통` 계산을 로그인된 Chrome으로 확인했습니다.
- 데스크톱 삭제 확인 문구가 두 줄로 접히는 것을 확인해 버튼 열 너비를 늘리고 한 줄 표시로 보정했습니다.
- 390px 브라우저 검증에서 배율에 따른 소수점 폭 때문에 정확히 390px인 미디어 쿼리가 적용되지 않는 경우를 확인해, 새 대화상자의 모바일 전환점을 420px로 넓혔습니다.
- 최종 공개 앱에서 데스크톱과 390px 모바일 버튼 배치, 삭제 미리보기와 오류 콘솔을 확인했습니다.
- J님의 확인 후 보정용 `2026-04-06 · 1통 입고` 기록을 영구 삭제하고, 남은 `2통 입고` 기록을 `3통`으로 수정했습니다.
- 운영 DB에서 해당 제품이 `0 → 3통` 입고 기록 1건과 현재 재고 3통으로 일치하며, 삭제 대상 기록이 남아 있지 않은 것을 확인했습니다.

## Next Steps - Inventory Event Replay Corrections

- 다른 제품에서 과거 입고·사용 기록을 수정할 때 미리보기의 결과 재고와 이후 재계산 건수를 확인한 뒤 저장합니다.
- 개봉·소진 기록이 쌓인 제품에서는 과거 입고 수정 후 사용 주기와 현재 재고가 함께 자연스럽게 이어지는지 실제 사용 흐름에서 관찰합니다.

## Work Summary - Restore Native Date Picker

단일 날짜 입력창에서 숫자와 슬래시를 직접 입력해야 했던 불편을 없애고, 기존처럼 터치로 달력·휠을 사용할 수 있도록 공통 날짜 입력 UI를 복구했습니다.

## Work Completed - Restore Native Date Picker

- `DateInput`을 텍스트 입력에서 네이티브 `input type="date"`로 변경했습니다.
- 저장에 사용하는 ISO 날짜값과 `min`·`max` 날짜 제한은 기존 방식 그대로 유지했습니다.
- 네이티브 달력 선택기가 보이도록 날짜 입력 스타일의 `appearance: none`을 제거했습니다.
- 구매일 수정, 개봉일, 사용 기록일, 사용 주기, 다음 세일 날짜 등 `DateInput`을 사용하는 단일 날짜 입력창에 같은 동작을 적용했습니다.
- 날짜 입력 안내를 달력 선택과 직접 입력을 모두 지원하는 문구로 바꿨습니다.
- 과거 구매일 여러 줄 입력용 textarea와 날짜 파서는 변경하지 않았으며, `26 → 2026` 규칙도 추가하지 않았습니다.

## Decisions - Restore Native Date Picker

- J님이 원하는 핵심은 짧은 연도 입력이 아니라 터치만으로 날짜를 고르는 편의성이므로, 단일 날짜 입력은 브라우저·기기의 네이티브 날짜 선택기를 우선 사용합니다.
- 과거 구매일 여러 줄 입력은 다른 사이트에서 복사해 붙여넣는 흐름을 보존하기 위해 기존 textarea를 유지합니다.

## Current Status - Restore Native Date Picker

- `npm.cmd run verify` 통과: Vite 빌드, 25개 테스트, Supabase 스키마 계약 검증.
- `git diff --check` 확인 예정입니다.
- 로컬 브라우저에서 로그인 화면이 정상 렌더링되고 콘솔 오류·경고가 없음을 확인했습니다.
- Google 로그인 세션이 없어 실제 대화상자에서 달력·휠을 여는 상호작용은 직접 확인하지 못했습니다.

## Next Steps - Restore Native Date Picker

- 로그인 가능한 모바일 브라우저에서 구매일·개봉일·사용 기록일 입력을 눌러 달력 또는 휠이 열리는지 확인합니다.
- 과거 구매일 여러 줄 입력이 기존 복사·붙여넣기 흐름대로 동작하는지 확인합니다.
