# 2026-08-15 Inventory Tracker Development Log

## Work Summary

앱에서 사용자가 보거나 직접 입력하는 날짜를 미국식 순서로 통일했습니다. 저장과 날짜 계산에 사용하는 ISO 형식은 유지해 기존 Supabase 데이터와 계산 로직의 호환성을 보존했습니다.

## Work Completed

- `formatDate()`와 날짜 입력 보조 함수를 정리해 화면 표시를 `M/D/YYYY` 형식으로 변경했습니다.
- `M/D/YYYY`와 `MM/DD/YYYY`를 모두 인식하는 공통 `DateInput` 컴포넌트를 추가했습니다.
- 구매일, 기록 날짜, 개봉일, 사용 주기 날짜 입력을 공통 텍스트 입력 방식으로 교체했습니다.
- 과거 구매일 여러 줄 입력의 안내 예시와 입력 설명을 미국식으로 변경했습니다.
- 기존 `YYYY-MM-DD`, 점 구분, 한글 날짜 입력은 호환용으로 계속 인식하도록 유지했습니다.
- 백업 파일명에 포함되는 날짜도 미국식 순서로 변경했습니다.
- 날짜 정규화·표시·잘못된 날짜·기존 형식 호환 회귀 테스트를 추가했습니다.

## Decisions

- Supabase의 `date` 컬럼과 앱 내부 날짜 계산은 `YYYY-MM-DD`를 계속 사용합니다. 표시 형식을 데이터 형식으로 바꾸면 정렬·기간 계산·기존 데이터에 불필요한 위험이 생기기 때문입니다.
- 화면에 보이는 기준 형식은 `M/D/YYYY`로 정하고, 입력 시에는 앞자리 0이 있는 `MM/DD/YYYY`도 허용합니다.
- 기기와 브라우저 지역 설정에 따라 표시가 달라지는 네이티브 날짜 입력 대신 공통 텍스트 입력을 사용해 앱이 정한 입력 순서를 일관되게 유지했습니다.

## Current Status

- `npm.cmd run verify` 통과: Vite 빌드, 19개 테스트, Supabase 스키마 계약 검증.
- `git diff --check` 통과.
- 로컬 브라우저에서 로그인 화면과 콘솔 오류 없음까지 확인했습니다.
- 현재 브라우저 세션에 Google 로그인 상태가 없어 인증 후 재고 화면의 날짜 다이얼로그를 여는 실제 상호작용 검증은 완료하지 못했습니다.
- 기존 작업 중이던 `src/components/ProductCard.tsx` 변경은 수정하지 않았습니다.

## Next Steps

- 로그인 가능한 브라우저 세션에서 데스크톱과 iPhone 크기로 구매일·입고일·사용일·개봉일·사용 주기 입력을 한 번씩 확인합니다.
- 실제 앱에서 `2/10/2024`, `02/10/2024`, 잘못된 날짜, 미래 날짜 입력 결과를 최종 확인합니다.

## Work Summary - Hide Store Names from Item Summary

아이템 리스트 카드의 요약 줄에서 쇼핑몰 이름을 숨겨 제품명과 현재 재고 중심으로 보이게 했습니다. 구매처 설정값과 구매처별 묶음 기능은 유지했습니다.

## Work Completed - Hide Store Names from Item Summary

- `ProductCard`의 축약 요약 영역에서 쇼핑몰 이름 목록 렌더링을 제거했습니다.
- 펼친 상세의 쇼핑몰 정보와 과거 구매 기록의 쇼핑몰 정보는 유지했습니다.
- `groupByStore()`가 제품의 `store_ids` 설정값을 기준으로 계속 그룹을 만드는지 기존 회귀 테스트로 확인했습니다.
- 빌드, 19개 테스트, 스키마 계약 검증, `git diff --check`를 다시 실행했습니다.

## Decisions - Hide Store Names from Item Summary

- 이번 요청은 첨부 화면의 축약 아이템 리스트에 한정해 구매처 표시를 숨기는 것으로 해석했습니다. 상세 화면의 쇼핑몰 정보까지 제거하면 설정값을 확인하기 어려워질 수 있어 보존했습니다.
- UI 표시와 그룹화 로직을 분리했습니다. 따라서 카드에서 구매처를 보이지 않게 해도 `구매처별 보기`는 `product.store_ids`와 쇼핑몰 설정을 기준으로 동작합니다.

## Current Status - Hide Store Names from Item Summary

- `npm.cmd run verify` 통과: Vite 빌드, 19개 테스트, Supabase 스키마 계약 검증.
- `git diff --check` 통과.
- 브라우저에서 로그인 화면과 콘솔 오류 없음까지 확인했습니다.
- 인증 세션이 없어 로그인 후 아이템 리스트에서 실제 구매처 문구가 사라지는 화면 상호작용은 미검증 상태입니다.

## Next Steps - Hide Store Names from Item Summary

- 로그인 가능한 브라우저 세션에서 아이템 리스트와 `구매처별 보기`를 직접 확인합니다.

## Work Summary - Lucide Lab Icon Integration

글자가 많은 재고 화면을 빠르게 구분할 수 있도록 J님이 선택한 Lucide Lab SVG를 앱 제목, 설정 메뉴, 카테고리 그룹 제목에 연결했습니다.

## Work Completed - Lucide Lab Icon Integration

- `public/icons`에 추가된 집, 설정, 장바구니, 화장품, 생활용품, 영양제, 의복, 미분류 아이콘 디자인을 `Icons.tsx`의 React SVG 컴포넌트로 반영했습니다.
- `Header`의 `우리집 재고` 왼쪽에 집 아이콘을 추가하고 기존 문구는 유지했습니다.
- 설정 메뉴 버튼의 기존 설정 아이콘을 `settings.svg` 디자인으로 교체했습니다.
- 카테고리 보기에서 식료품·화장품·생활용품·영양제·의복·미분류 그룹명 앞에 각각 지정한 아이콘을 추가했습니다.
- 쇼핑몰별 보기에서는 쇼핑몰 이름 앞에 카테고리 아이콘이 붙지 않도록 조건을 분리했습니다.
- 데스크톱과 모바일에서 제목·카테고리 아이콘 크기가 화면에 맞도록 스타일을 추가했습니다.

## Decisions - Lucide Lab Icon Integration

- SVG를 단순 이미지로 표시하지 않고 기존 인라인 SVG 아이콘 구조에 맞춰 연결했습니다. 헤더의 흰색 아이콘과 카테고리별 색상·크기를 `currentColor`로 일관되게 제어할 수 있기 때문입니다.
- 아이콘은 의미를 보조하는 장식 요소로 처리하고 카테고리명과 앱 이름 텍스트는 유지했습니다. 따라서 아이콘이 로드되지 않거나 색상이 달라져도 핵심 정보는 사라지지 않습니다.

## Current Status - Lucide Lab Icon Integration

- `npm.cmd run verify` 통과: Vite 빌드, 19개 테스트, Supabase 스키마 계약 검증.
- 브라우저에서 앱 로그인 화면이 정상 렌더링되고 콘솔 오류·경고가 없음을 확인했습니다.
- 현재 브라우저 세션에 Google 로그인 상태가 없어 인증 후 재고 목록의 실제 카테고리 아이콘 배치는 직접 확인하지 못했습니다.

## Next Steps - Lucide Lab Icon Integration

- 로그인 가능한 브라우저 세션에서 카테고리 보기와 쇼핑몰별 보기를 데스크톱·모바일 크기로 한 번씩 확인합니다.

## Work Summary - Store Icon Integration

주구매처별 목록도 빠르게 구분할 수 있도록 쇼핑몰 그룹 이름 앞에 J님이 지정한 동물·자연물 아이콘을 추가했습니다.

## Work Completed - Store Icon Integration

- 로컬에 없던 `bird.svg`, `fish.svg`, `squirrel.svg`, `snail.svg`, `feather.svg`, `birdhouse.svg`, `shell.svg`를 공식 Lucide 아이콘 원본 기준으로 `public/icons`에 추가했습니다.
- 쿠팡·네이버·마켓컬리·아이허브·올리브영·자사몰·기타를 각각 지정된 아이콘 컴포넌트에 매핑했습니다.
- 쇼핑몰별 보기의 그룹 제목 앞에 주구매처 아이콘을 표시하고, 미지정 쇼핑몰은 기타용 조개 아이콘을 사용하도록 했습니다.
- 카테고리별 보기의 기존 아이콘과 쇼핑몰별 보기의 새 아이콘을 분리해 각 보기의 의미가 섞이지 않도록 유지했습니다.

## Decisions - Store Icon Integration

- 주구매처 아이콘도 기존 카테고리 아이콘과 동일하게 인라인 SVG 컴포넌트로 연결했습니다. 헤더·카테고리 아이콘과 같은 `currentColor` 제어와 크기 규칙을 공유하기 위해서입니다.
- 현재 저장된 쇼핑몰 이름이 지정 목록에 없더라도 목록이 깨지지 않도록 `기타` 아이콘을 안전한 기본값으로 사용했습니다.

## Current Status - Store Icon Integration

- 공식 Lucide SVG 원본을 기준으로 아이콘 파일과 React 매핑을 추가했습니다.
- `npm.cmd run verify` 통과: Vite 빌드, 19개 테스트, Supabase 스키마 계약 검증.
- 개발 서버에서 새 SVG 7개가 모두 정상 제공되는 것을 확인했습니다.
- 브라우저에서 로그인 화면과 콘솔 오류·경고 없음까지 확인했습니다.
- 로그인 세션이 없어 실제 쇼핑몰별 보기에서 그룹 앞 아이콘이 보이는 화면은 아직 직접 확인하지 못했습니다.

## Next Steps - Store Icon Integration

- 로그인 가능한 브라우저 세션에서 쇼핑몰별 보기의 각 그룹 아이콘과 카테고리별 보기의 기존 아이콘이 서로 바뀌지 않는지 데스크톱·모바일 크기로 확인합니다.

## Work Summary - Expanded Card Scroll Position

아이템 카드를 펼쳤을 때 긴 상세 영역의 아래쪽이 화면에 남아 무엇을 보고 있는지 헷갈리는 문제를 줄이기 위해 펼쳐진 카드의 상단으로 화면을 이동하도록 수정했습니다.

## Work Completed - Expanded Card Scroll Position

- `ProductCard`가 펼침 상태가 된 다음 한 프레임 뒤 카드 자체를 화면 상단 기준으로 스크롤하도록 처리했습니다.
- 상단 고정 헤더에 카드 제목이 가려지지 않도록 데스크톱·모바일별 `scroll-margin-top`을 추가했습니다.
- `prefers-reduced-motion` 설정이 켜진 환경에서는 부드러운 이동 대신 즉시 이동하도록 처리했습니다.

## Decisions - Expanded Card Scroll Position

- 키보드 포커스를 다른 요소로 강제로 옮기지 않고, 클릭한 카드의 상단이 보이도록 화면 스크롤만 조정했습니다. 따라서 기존의 펼침 버튼 접근성과 키보드 탐색 순서는 유지됩니다.
- 카드가 닫힐 때는 스크롤하지 않고, 카드가 펼쳐질 때만 이동하도록 제한했습니다.

## Current Status - Expanded Card Scroll Position

- `npm.cmd run verify` 통과: Vite 빌드, 19개 테스트, Supabase 스키마 계약 검증.
- `git diff --check` 통과.
- 브라우저에서 로그인 화면과 콘솔 오류·경고 없음까지 확인했습니다.
- 로그인 가능한 세션이 없어 실제 아이템 카드를 클릭한 뒤 카드 상단으로 이동하는 상호작용은 직접 확인하지 못했습니다.

## Next Steps - Expanded Card Scroll Position

- 로그인 가능한 브라우저 세션에서 화면 아래쪽의 아이템을 클릭해 카드 상단과 고정 헤더 사이에 제목이 보이는지 데스크톱·모바일 크기로 확인합니다.

## Work Summary - Collapse Groups by Default

빨간 상태 아이템이 있는 카테고리가 자동으로 펼쳐져 목록이 산만해지는 문제를 줄이기 위해, 카테고리와 주 구매처 그룹을 기본적으로 모두 접힌 상태로 표시하도록 수정했습니다.

## Work Completed - Collapse Groups by Default

- 빨간 상태 아이템 수 또는 `확인 필요` 필터를 기준으로 그룹을 자동 펼치던 조건을 제거했습니다.
- 첫 화면에서는 모든 카테고리·주 구매처 그룹이 접힌 상태로 시작하도록 변경했습니다.
- 검색어 입력, 그룹 제목을 직접 클릭하는 동작, 아이템 카드 펼침처럼 사용자가 명시적으로 내용을 확인하는 경우에는 기존처럼 필요한 그룹이 열리도록 유지했습니다.
- 그룹 제목에 표시되는 빨간 상태 아이콘과 `확인` 개수는 그대로 유지하고, 자동 펼침 동작만 변경했습니다.

## Decisions - Collapse Groups by Default

- 빨간 상태 아이템은 재고 데이터상 항상 존재할 수 있으므로, 해당 상태를 그룹 열림의 기본 조건으로 사용하지 않도록 했습니다.
- 사용자가 검색하거나 직접 그룹을 열었을 때만 내용을 펼쳐 정보량을 제어하고, 상태 요약은 접힌 그룹 제목에서 계속 확인할 수 있게 했습니다.

## Current Status - Collapse Groups by Default

- `npm.cmd run verify` 통과: Vite 빌드, 19개 테스트, Supabase 스키마 계약 검증.
- `git diff --check` 통과.
- 로컬 브라우저 로그인 화면 렌더링과 콘솔 오류·경고 없음까지 확인했습니다.
- Google 로그인 세션이 없어 실제 재고 목록에서 그룹을 클릭하는 상호작용 검증은 아직 완료하지 못했습니다.

## Next Steps - Collapse Groups by Default

- 로그인 가능한 브라우저 세션에서 카테고리별 보기와 주 구매처별 보기의 초기 상태가 모두 접혀 있는지 확인합니다.
- 검색·수동 그룹 열기·아이템 카드 펼침 시 해당 그룹만 정상적으로 열리는지 확인합니다.
