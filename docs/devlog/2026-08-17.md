# 2026-08-17 Inventory Tracker Development Log

## Work Summary

모바일 기록 화면에서 구매일·수량·용량·구매처가 여러 줄로 나뉘어 스크롤이 길어지는 문제를 줄이기 위해 구매 정보 표시를 한 줄 우선 구조로 정리했습니다.

## Work Completed

- `기록과 근거`의 최근 구매 기록에서 날짜·수량·구매처를 하나의 한 줄 정보 묶음으로 표시했습니다.
- `전체 구매 기록`의 날짜·수량·용량·구매처를 하나의 가로 정보 묶음으로 표시했습니다.
- 모바일 폭이 부족할 때는 긴 구매처 이름만 말줄임 처리하고, 날짜·수량·용량 정보가 구매처 앞에서 줄바꿈되지 않도록 했습니다.
- 기록과 근거 화면의 재고·사용 기록 표시 방식은 변경하지 않았습니다.

## Decisions

- 줄바꿈을 단순히 전체 대화상자에 허용하지 않고 구매 정보 단위를 하나로 묶었습니다. 모바일에서 구매처만 다음 줄로 밀리는 문제를 직접 방지하면서 기존 클릭 가능한 행 구조는 유지하기 위해서입니다.
- 아주 긴 구매처 이름까지 강제로 화면 밖으로 내보내지 않도록 구매처 텍스트에만 말줄임을 적용했습니다.

## Current Status

- `npm.cmd run verify` 통과: Vite 빌드, 31개 테스트, Supabase 스키마 계약 검증.
- 모바일 viewport 390px에서 로컬 로그인 화면의 정상 렌더링과 콘솔 오류·경고 없음을 확인했습니다.
- Google 로그인 세션이 없어 실제 `기록과 근거`와 `전체 구매 기록` 대화상자 내부의 모바일 줄바꿈 결과는 직접 확인하지 못했습니다.

## Next Steps

- 로그인 가능한 모바일 환경에서 두 기록 화면을 열어 날짜·수량·용량·구매처가 한 줄로 표시되는지 확인합니다.

## Work Summary - 모바일 재고·용량 카드 2열 유지

제품 카드가 모바일에서 `현재 재고`와 `제품 1개 용량`을 세로로 쌓아 불필요하게 길어지는 문제를 확인하고, 짧은 정보 카드가 PC처럼 가로로 유지되도록 반응형 규칙을 조정했습니다.

## Work Completed - 모바일 재고·용량 카드 2열 유지

- `product-overview-grid`가 350px 이하 화면에서 1열로 바뀌던 예외를 제거했습니다.
- `현재 재고`와 `제품 1개 용량` 카드가 가장 좁은 모바일 폭에서도 기존 2열 그리드로 표시되도록 했습니다.
- 통계 카드와 기록 액션의 좁은 화면 동작은 이번 요청 범위에 맞춰 그대로 유지했습니다.

## Decisions - 모바일 재고·용량 카드 2열 유지

- 두 카드의 내용이 짧고 서로 비교해서 보는 정보이므로, 모바일에서도 카드 폭을 나누는 것이 화면 길이를 줄이고 PC와 동일한 정보 구조를 유지하는 데 적합하다고 판단했습니다.
- 다른 그리드까지 일괄적으로 2열로 고정하지 않고 `product-overview-grid`만 대상으로 삼아 좁은 화면에서 통계·액션 버튼이 과도하게 눌리는 것을 피했습니다.

## Current Status - 모바일 재고·용량 카드 2열 유지

- `npm.cmd run verify` 통과: Vite 빌드, 31개 테스트, Supabase 스키마 계약 검증.
- `git diff --check` 통과했습니다. 기존 줄바꿈 형식에 대한 Git 경고만 확인되었습니다.
- 로컬 앱을 390×844 모바일 viewport에서 확인했으며 로그인 화면은 콘솔 오류·경고 없이 렌더링되었습니다.
- Google 로그인 세션이 없어 실제 제품 카드를 펼친 상태의 모바일 화면 비교는 완료하지 못했습니다.

## Next Steps - 모바일 재고·용량 카드 2열 유지

- 로그인 가능한 모바일 환경에서 제품 카드를 펼쳐 두 정보 카드가 한 줄에 표시되는지 최종 확인합니다.
