# 2026-08-16 Inventory Tracker Development Log

## Work Summary

아이템 하나를 펼쳤을 때 제품 정보, 통계, 구매/재고/사용 기록이 모두 이어져 모바일 스크롤이 지나치게 길어지는 문제를 개선했다. 메인 카드는 현재 상태와 핵심 통계 및 기록 버튼 중심의 대시보드로 줄이고, 상세 통계와 기록 근거는 별도 다이얼로그로 분리했다.

## Work Completed

- 펼친 제품 카드에서 현재 재고, 제품 1개 용량, 월평균 소비량, 1년 예상 필요량, 다음 세일 구매 추천을 우선 표시하도록 정보 계층을 재구성했다.
- 입고, 사용/소진, 재고 정정, 구매 기록 버튼은 카드에서 바로 접근할 수 있도록 유지했다.
- 상세 통계 다이얼로그를 추가하고 재고/사용 기준과 구매/소비 통계를 한곳에서 확인할 수 있게 했다.
- 기록과 근거 다이얼로그에 구매, 재고, 사용 탭을 추가하고 최근 기록과 전체 기록/수정 화면 진입점을 연결했다.
- 제품의 고정 정보는 제품 정보/설정 버튼으로 기존 편집 화면에 연결했다.
- 이번 카드 개편 범위의 표기에서 가운뎃점을 제거하고 나열은 `/`, 환산 및 보충 정보는 `()`, 날짜 뒤의 부가 정보는 쉼표를 사용했다.
- 표기 형식 회귀 테스트를 추가했다.
- `npm.cmd run verify`로 빌드, 26개 단위 테스트, 스키마 계약 검증을 완료했다.
- 실제 ProductCard에 세럼 형태의 모의 데이터를 넣어 카드와 상세 다이얼로그를 렌더링하고 탭 전환, 가로 넘침, 콘솔 오류, 카드 내 가운뎃점 유무를 확인했다.

## Decisions

- 모든 정보를 카드 안에서 접는 방식보다, 자주 보는 핵심 정보만 카드에 남기고 상세 정보는 독립 다이얼로그로 분리하는 점진적 공개 방식을 선택했다. 이 방식이 카드 높이를 줄이면서도 통계와 원본 기록의 접근성을 보존한다.
- 구매 주기는 보조 통계로 상세 화면에 남기고, 월평균 소비량과 연간 필요량 및 세일 구매 추천을 메인 카드의 핵심 통계로 올렸다.
- 표기 정책은 전체 앱에 한꺼번에 적용하지 않고 이번에 새로 구성한 카드와 상세 다이얼로그 범위에만 적용했다. 나머지 화면의 가운뎃점은 별도 작업으로 남겼다.
- 구매/재고/사용 기록을 한 화면에 다시 길게 나열하지 않고 탭으로 구분해 한 번에 한 종류의 근거만 보이도록 했다.

## Current Status

제품 카드 개편, 상세 통계/기록 다이얼로그, 표기 규칙 적용과 자동 검증이 완료됐다. 모의 데이터 렌더링에서는 가로 넘침과 콘솔 오류가 없었고 카드 및 다이얼로그에 가운뎃점이 남지 않았다. 로그인된 운영 데이터에서의 최종 확인은 GitHub Pages 배포 후 별도로 필요하다.

## Next Steps

- GitHub Pages 배포 결과와 운영 자산을 확인한다.
- J님의 실제 iPhone 화면에서 카드 높이와 버튼 밀도를 확인하고 필요하면 여백을 추가 조정한다.
- 이번 범위 밖의 가운뎃점을 새 표기 정책에 맞춰 단계적으로 정리한다.

## Work Summary - 계절 제품 사용 시기와 소비 통계

연간 소비량을 12개월로 나눈 기존 월평균만으로는 여름 토너나 겨울 음식의 실제 사용 월 소비 속도와 시즌 구매량을 판단하기 어려웠다. 제품별 사용 월을 최소 설정으로 추가하고, 비사용 월을 제외해 소비량·소진일·세일 구매 추천을 계산하도록 통계 모델을 확장했다.

## Work Completed - 계절 제품 사용 시기와 소비 통계

- 제품 설정에 기본 `연중 사용`과 선택형 `특정 달에만 사용`을 추가하고, 계절 제품은 1~12월 체크박스로 활성 월을 선택하도록 구성했다.
- Supabase 제품 데이터에 선택적 활성 월을 저장하는 migration과 생성·수정 RPC 오버로드를 추가했다. 기존 RPC는 이전 클라이언트 호환을 위해 유지했다.
- 과거 구매량은 관찰 기간의 활성 일수로 나누고, 사용 기록 간격과 개봉–소진 기간에서도 비사용 월을 제외하도록 계산을 수정했다.
- 소진 예상일은 비사용 월을 건너뛰며, 세일 전 예상 소비량과 확보 기간 필요량도 활성 일수만 반영하도록 수정했다.
- 계절 제품 통계에 연평균 월 환산량, 사용 월 평균 소비량, 연간 예상 필요량, 사용 시즌 예상량을 구분해 표시했다.
- 계절 제품과 구매 시기의 도메인 구분을 `CONTEXT.md`에 기록하고, 로직 및 스키마 계약 테스트를 추가했다.
- `npm.cmd run verify`로 프로덕션 빌드, 28개 단위 테스트, 정적 스키마 계약 검증을 완료했다.

## Decisions - 계절 제품 사용 시기와 소비 통계

- 월별 소비량을 12개 수치로 직접 입력하게 하지 않고, 실제 사용하는 달만 입력하면 기존 기록에서 나머지 통계를 파생하도록 입력 부담을 제한했다.
- `active_months = null`은 연중 사용을 뜻하고, 계절 제품만 1~11개의 고유 월을 저장하도록 했다.
- 사용 월과 구매·세일 월은 서로 다른 개념으로 유지했다. 구매 기록 시점은 비축 구매의 영향을 받으므로 계절성을 자동 추론하는 근거로 사용하지 않았다.
- 기존 월평균은 연평균 월 환산량으로 보존하고, 계절 제품의 실제 속도는 사용 월 평균으로 별도 표시해 연간 비교와 시즌 계획을 모두 지원했다.

## Current Status - 계절 제품 사용 시기와 소비 통계

코드, UI, migration, 도메인 문서와 자동 검증은 완료됐다. 로그인 전 화면의 로드, 제목, 콘솔 무오류는 브라우저에서 확인했으나 인증 세션이 없어 제품 설정 다이얼로그의 실제 렌더링과 저장 흐름은 확인하지 못했다. Docker 또는 Podman이 설치되어 있지 않아 로컬 Supabase pgTAP은 실행하지 못했으며, migration은 아직 운영 DB에 적용하지 않았다.

## Next Steps - 계절 제품 사용 시기와 소비 통계

- 로그인된 환경에서 월 선택, 저장, 재진입 및 계절 통계 표시를 확인한다.
- 로컬 Supabase 또는 배포 전 검증 환경에서 migration 재생과 pgTAP 41개 계약을 실행한다.
- 운영 DB에 migration을 적용한 뒤 프런트엔드와 함께 배포하고, 실제 더랩 토너 데이터로 연평균·사용 월 평균·시즌 필요량을 확인한다.

## Work Summary - 계절 제품 운영 DB 반영

계절 제품 기능을 프런트엔드보다 먼저 안전하게 수용할 수 있도록 운영 Supabase 스키마를 선반영했다. 기존 앱과 데이터의 동작은 유지하면서 새 버전 배포 시 활성 월을 저장할 준비를 마쳤다.

## Work Completed - 계절 제품 운영 DB 반영

- `mworkroom` 운영 프로젝트에 `add_product_active_months` migration을 적용했다.
- 원격에서 발급된 migration 버전 `20260815163118`에 로컬 migration 파일과 정적 스키마 계약을 맞췄다.
- 기존 상품 26개가 모두 `active_months = null`로 유지돼 기존 의미인 연중 사용으로 해석되는지 확인했다.
- 새 열과 검증 제약, 제품 생성·수정 RPC 오버로드가 존재하는지 확인했다.
- 새 RPC는 로그인 사용자만 실행할 수 있고 익명 사용자는 실행할 수 없으며, 로그인 사용자에게 `active_months` 직접 수정 권한이 열리지 않았음을 확인했다.
- Supabase 보안·성능 Advisor를 다시 확인했다. 이번 migration에 의해 익명 RPC 실행이나 새 테이블 보안 경고가 추가되지는 않았고, 공유 프로젝트의 기존 경고는 이번 기능 범위 밖이라 변경하지 않았다.

## Decisions - 계절 제품 운영 DB 반영

- 저장소에 Supabase CLI 연결 설정이 없으므로 연결된 관리 경로로 migration을 적용하고, 원격 이력의 버전을 로컬 파일명에 반영해 이후 migration 순서를 일치시켰다.
- 실제 상품을 변경하는 운영 쓰기 테스트는 수행하지 않았다. 카탈로그·권한 조회로 스키마 계약을 확인하고 실제 저장 흐름은 로그인된 UI 검증 단계로 분리했다.

## Current Status - 계절 제품 운영 DB 반영

운영 DB 스키마 반영과 데이터 보존·권한 검증은 완료됐다. 프런트엔드 코드는 아직 로컬 변경 상태이며 GitHub Pages에는 배포되지 않았다.

## Next Steps - 계절 제품 운영 DB 반영

- 로컬 전체 검증을 다시 실행한 뒤 관련 파일만 커밋하고 `main`에 푸시한다.
- GitHub Actions의 격리 DB pgTAP과 Pages 배포가 완료되는지 확인한다.
- 로그인된 운영 화면에서 실제 계절 상품의 월 선택, 저장, 재진입과 통계 값을 확인한다.

## Work Summary - 계절 제품 기능 배포 완료

운영 DB에 선반영한 계절 제품 스키마와 프런트엔드 기능을 `main`에 병합하고 GitHub Pages에 배포했다. PR과 main 양쪽에서 자동 검증을 통과한 뒤 공개 자산까지 확인했다.

## Work Completed - 계절 제품 기능 배포 완료

- PR #10을 스쿼시 병합해 `main` 커밋 `0617e9e`로 반영했다.
- PR에서 일반 검증과 격리 Supabase pgTAP 계약 검사를 통과했다.
- main 병합 후 데이터베이스 계약 워크플로와 GitHub Pages 빌드·배포·실서비스 자산 확인이 모두 통과했다.
- 공개 페이지와 JavaScript 자산이 HTTP 200을 반환하고, 배포 번들에 `p_active_months`, `특정 달에만 사용`, `사용 월 평균`, `사용 시즌 예상량`이 포함됐음을 확인했다.
- 인앱 브라우저에서 운영 로그인 화면의 제목, 본문, 버튼 활성·키보드 초점과 콘솔 무오류를 확인했다.

## Decisions - 계절 제품 기능 배포 완료

- 운영 DB를 하위 호환 방식으로 먼저 반영한 뒤 프런트엔드를 배포해 구버전과 신버전 클라이언트가 전환 중에도 함께 동작하도록 했다.
- 인증을 시작하는 브라우저 동작은 수행하지 않고, 로그인 버튼의 표시·활성·키보드 초점까지만 확인했다. 실제 제품 저장은 로그인된 데이터 검증으로 남겼다.

## Current Status - 계절 제품 기능 배포 완료

운영 DB, `main`, GitHub Pages와 공개 번들 반영이 완료됐다. 로그인된 운영 데이터에서 활성 월 저장·재진입과 실제 더랩 토너 통계 값 확인만 남아 있다.

## Next Steps - 계절 제품 기능 배포 완료

- 로그인된 운영 화면에서 그린 토너의 사용 월을 6~8월로 설정하고 저장·재진입을 확인한다.
- 실제 기록을 기준으로 연평균, 사용 월 평균과 사용 시즌 예상량이 의도한 값인지 확인한다.

## Work Summary - Filter-Specific Product Sorting

전체 제품 목록에서는 제품명을 가나다순으로 빠르게 찾고, `확인 필요` 목록에서는 기존 중요도 중심 순서를 유지할 수 있도록 필터별 제품 정렬을 분리했습니다.

## Work Completed - Filter-Specific Product Sorting

- `전체` 필터의 제품 정렬을 제품명 가나다순으로 변경했습니다.
- `확인 필요` 필터는 기존 `재고·소진 중요도 → 재구매 필요 → 제품명` 순서를 그대로 유지했습니다.
- 카테고리별 보기와 주 구매처별 보기 모두 동일한 제품 정렬 결과를 각 그룹 안에서 사용하도록 공통 뷰 모델 정렬 분기만 수정했습니다.
- 검색어가 있는 상태에서도 선택된 필터의 정렬 규칙이 유지되도록 했습니다.

## Decisions - Filter-Specific Product Sorting

- 그룹 자체의 순서(카테고리명 또는 주 구매처 설정 순서)는 변경하지 않고, 그룹 안의 아이템 순서만 필터에 따라 바꿨습니다.
- 기존 `확인 필요` 정렬 함수를 재사용해 중요도 순서가 바뀌지 않도록 했습니다.

## Current Status - Filter-Specific Product Sorting

- `npm.cmd run verify` 통과: Vite 빌드, 28개 테스트, Supabase 스키마 계약 검증.
- 로컬 브라우저에서 로그인 화면이 정상 렌더링되고 콘솔 오류·경고가 없음을 확인했습니다.
- Google 로그인 세션이 없어 실제 목록에서 `전체`와 `확인 필요` 필터를 전환하는 화면 상호작용은 직접 확인하지 못했습니다.

## Next Steps - Filter-Specific Product Sorting

- 로그인된 화면에서 전체 목록이 가나다순인지 확인합니다.
- `확인 필요` 목록에서 기존 중요도순이 유지되고, 같은 중요도 안에서 가나다순으로 정렬되는지 확인합니다.

## Work Summary - 소비 속도 우선 재고 알림

실제 사용 기록이 없는 제품도 과거 구매량으로 월·연간 소비량을 계산하고 있었지만, 해당 속도가 재고 알림에는 연결되지 않아 느리게 쓰는 대용량 제품이 기본 수량 기준으로 너무 일찍 표시되는 문제를 개선했다. 재고 알림은 가장 신뢰할 수 있는 소비 속도에 따른 소진 예측을 우선하고, 수량 기준과 구매 간격은 근거가 부족할 때 사용하는 보조 신호로 재정리했다.

## Work Completed - 소비 속도 우선 재고 알림

- 제품 분석 단계에서 소비 통계와 소진 예측을 함께 계산해 구매량 기반 소비 속도를 현재 재고의 예상 소진일에 연결했다.
- 실제 사용 기록을 최우선으로 유지하고, 구매 기록 3건 이상·관찰 기간 180일 이상·호환되는 용량 기록 조건을 충족한 과거 구매량만 소진 예측에 사용하도록 제한했다.
- 신뢰 가능한 소진 예측이 있으면 기본 수량 기준과 단순 구매 간격 신호가 앞서지 않도록 알림 우선순위를 변경했다.
- 사용 중인 포장 제품은 개봉일부터 지난 활성 사용일을 구매량 기반 예상 사용 기간에서도 차감하도록 했다.
- 제품 카드와 상세 통계에 `과거 구매량으로 추정` 근거를 표시하고, 제품 설정의 수량 기준이 소진 예측이 없을 때 사용하는 안전망임을 명확히 했다.
- `CONTEXT.md`에 소비 속도 근거, 소진 예측, 수량 안전망과 재구매 시기 신호를 구분해 기록했다.
- 더랩 히알루론산 토너, Oats Thick, 실제 사용 기록 우선과 구매량 근거 부족 상황을 회귀 테스트로 추가했다.
- `npm.cmd run verify`로 프로덕션 빌드, 단위 테스트 31개와 정적 스키마 계약 검증을 완료했다.

## Decisions - 소비 속도 우선 재고 알림

- 구매 간격은 비축 구매나 세일의 영향을 받으므로 소비 속도로 취급하지 않고, 소진 예측을 만들 수 없을 때만 재구매 시기 보조 신호로 유지했다.
- 과거 구매량 통계는 화면에 표시할 수 있는 최소 조건보다 알림에 사용할 조건을 더 엄격하게 설정했다. 장기간·복수 기록이 있을 때만 재고 위험 판단에 사용해 경계 재고 오차의 영향을 줄였다.
- 기존 수량 입력 UI와 데이터 구조는 유지하되 의미를 항상 적용되는 임계치에서 소진 예측이 없을 때의 안전망으로 좁혔다. 별도 설정이나 DB migration은 추가하지 않았다.

## Current Status - 소비 속도 우선 재고 알림

계산 로직, UI 문구, 도메인 문서와 회귀 테스트 수정은 로컬에서 완료됐고 전체 자동 검증이 통과했다. 로컬 브라우저에서 로그인 화면의 렌더링, 버튼 키보드 초점과 콘솔 무오류를 확인했다. 인증 세션이 없어 실제 제품 카드 렌더링은 확인하지 못했으며 운영 배포도 아직 수행하지 않았다.

## Next Steps - 소비 속도 우선 재고 알림

- 로컬 렌더링에서 구매량 기반 소진 예측 문구와 제품 설정 안내를 확인한다.
- 로그인된 운영 데이터에서 더랩 히알루론산 토너와 Oats Thick의 알림 상태가 예상대로 바뀌는지 확인한다.
- 별도 작업 중인 정렬·파비콘 변경과 범위를 분리해 커밋 및 배포 여부를 결정한다.
